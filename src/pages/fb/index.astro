---
/* /fb/ was the first Instant Form follow-up page; any form still linking here gets the turf cleaning version. */
import FollowUp from '../../components/campaign/FollowUp.astro';
import { findAngle } from '../../data/campaigns';
---
<FollowUp a={findAngle('clean')!} />
